///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace UpchainMechDeployTest
{
    public partial class GetFileVersions
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        /// 
        
        public static List <Tuple<string, int, int>> fileVersionList = new List<Tuple<string, int, int>>();
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void GetFileVersionsofCells()
        {
        	fileVersionList = UserCodeMethods.GetFileNameandVersionFromTable(repo.PLMMainForm.MainSplitPane.designTreeTable, "Item-Document", "Version");
        	foreach (var listitem in fileVersionList) 
            {
            	
            	Report.Log(ReportLevel.Info, "Cell: " + listitem.Item1 + " with Row Index: " + listitem.Item2 + " has File Version: " + listitem.Item3.ToString("D3"));
            }
        	/* var dataTable = repo.PLMMainForm.MainSplitPane.designTreeTable;
            var cols = dataTable.Columns;
            var rows = dataTable.Rows;
//            var tuplist = new List<Tuple<string, int, int>>();
            int colIndex = -1;
            int strtoNum = 0;
            var dict = new Dictionary<string, int>();
            foreach (var col in cols) 
            {
            	if (col.Text == "Item-Document")
            	{
            		foreach (var cell in col.Cells) 
            		{
            			if (cell.Text.Contains("AP"))
            			    {
            			    	dict.Add(cell.Text, cell.RowIndex);
            			    }
            		}
            	}
            	if (col.Text == "Version")
            	{
            		colIndex = col.Index;
            	}
            }
            
            foreach (var row in rows) 
            {
            	foreach (var kvp in dict) 
            	{
            		if (row.Index == kvp.Value)
            		{
						foreach (var cell in row.Cells) 
						{
							if (cell.ColumnIndex == colIndex)
							{						
								strtoNum = Int32.Parse(cell.Text);
								tuplist.Add(Tuple.Create(kvp.Key, kvp.Value, strtoNum));
							}
						}            			
            		}	
            		
            	}
            	
            	
            }
            foreach (var listitem in tuplist) 
            {
            	
            	Report.Log(ReportLevel.Info, "Cell: " + listitem.Item1 + " with Row Index: " + listitem.Item2 + " has File Version: " + listitem.Item3.ToString("D2"));
            }*/
            

        }

    }
}
