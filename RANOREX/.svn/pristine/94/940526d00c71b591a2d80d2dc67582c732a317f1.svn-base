///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
// 
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Drawing;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading;
using System.Diagnostics;
using WinForms = System.Windows.Forms;
using System.IO;
using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace ATS_Document_Processor
{
    public partial class RepairExcel
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void SetFilePath(string ProjectName, string OutputRootFolder, string OutputSubFolder, string ProjectSubFolder, string FileName)
        {
        	FilePath = "\"" + System.IO.Path.Combine(OutputRootFolder, OutputSubFolder, ProjectName, ProjectSubFolder, FileName) + "\"";
        	Report.Log(ReportLevel.Info, "Set FilePath = " + FilePath);
        }

        public void KillProcess(string ProcessName)
        {
        	Thread.Sleep(1000);
        	foreach (var process in Process.GetProcessesByName(ProcessName))
			{
        		try {
			    	process.Kill();        				
        		} catch (Exception ex) {
        			Report.Log(ReportLevel.Info, ex.Message);
        		}
			}
        }

        public void OpenExcelFile()
        {
            Process.Start(FilePath);
            Report.Log(ReportLevel.Info, "Opening " + FilePath);
        }

        public void DeleteAndRenameExcel()
        {
        	var FilePathWithoutQuotes = System.IO.Path.Combine(OutputRootFolder, OutputSubFolder, ProjectName, ProjectSubFolder, FileName);
        	Report.Info("FilePathWithoutQuotes= " + FilePathWithoutQuotes);
        	if(File.Exists(FilePathWithoutQuotes))
        	{
        		File.Delete(FilePathWithoutQuotes);
        		Delay.Seconds(5);
        		var FullNewFilePath = System.IO.Path.Combine(OutputRootFolder, OutputSubFolder, ProjectName, ProjectSubFolder, varNewFileName);
        		Report.Info("FullNewFilePath= " + FullNewFilePath);
        		File.Move(FullNewFilePath,FilePathWithoutQuotes);
        		Report.Info("Successfully renamed " + FullNewFilePath + " to " + FilePathWithoutQuotes);
        	}
        	else
        		Report.Warn("Original File Path doesnt exist");
        	
        }

    }
}