///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
// 
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Testing;

namespace PLMWorxVaultUpdate
{
    public partial class ReviewResults
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void LoadItem(string FileVersion, string Filename, string Extension)
        {
        	if (!string.IsNullOrEmpty(AssociatedItem))
        	{          	
        		Report.Log(ReportLevel.Info, AssociatedItem);
        		
        		ValidateItem.Instance.VersionRowNumber = (int.Parse(VersionRowCount) - int.Parse(FileVersion)).ToString();    
            	ValidateItem.Instance.Filename = System.IO.Path.GetFileNameWithoutExtension(Filename);
            	ValidateItem.Instance.FileExtension = Extension;
            	ValidateItem.Instance.FileVersion = FileVersion;
            	Report.Log(ReportLevel.Info, "Filename = " + ValidateItem.Instance.Filename + ", FileExtension = " + ValidateItem.Instance.FileExtension + ", FileVersion = " + ValidateItem.Instance.FileVersion);
            	
            	ValidateItem.Start();
        	}
        	else
        	{
        		Report.Log(ReportLevel.Info, "This file does not have an associated item item");
        	}
        }

        public void GetVersionHistoryRowNumber(string FileVersion, string VersionRowCount)
        {
        	VersionRowNumber = (int.Parse(VersionRowCount) - int.Parse(FileVersion)).ToString();        		
        	Report.Log(ReportLevel.Info, "VersionRowCount = " + VersionRowCount + ", FileVersion = " + FileVersion + ", VersionRowNumber = " + VersionRowNumber);
            	
        }     
    }
}