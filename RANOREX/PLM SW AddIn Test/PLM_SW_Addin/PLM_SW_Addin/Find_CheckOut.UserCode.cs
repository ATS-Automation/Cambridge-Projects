///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;
using System.Diagnostics;
using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace PLM_SW_Addin
{
    public partial class Find_CheckOut
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void find_Check_Out(string cellTextofFileName, string columnName)
        {
            Form PLM = @"/form[@title~'^Connected\ to\ ESR']";
            string tableXPath = @".//table[@name='designTreeTable']";
            Stopwatch sw = Stopwatch.StartNew();
            int rowIndexofFileName = 0;
            Ranorex.Cell checkOut;
            bool checkOutButtonFound = false;
            bool columnNameFound = false;
            bool cellTextofFileNameFound = false;
            try 
            {
            	Ranorex.Table checkoutTable = PLM.FindSingle<Ranorex.Table>(tableXPath, Duration.FromMilliseconds(120000));
            	Report.Screenshot(checkoutTable);
            	if (checkoutTable.Visible == true)
            	{
            		var rowsInTable = checkoutTable.Rows;
            		foreach (var row in rowsInTable) 
            		{
            			var cellsinRow = row.Cells;
            			
            			foreach (var cell in cellsinRow) 
            			{
            				if (cell.Text == cellTextofFileName)
            				{
            					rowIndexofFileName = cell.RowIndex;
            					cellTextofFileNameFound = true;
            				}
            			}
            		}
            		
            		var colsInTable = checkoutTable.Columns;
            		foreach (var col in colsInTable) 
            		{
            			if (col.Text == columnName)
            			{
            				columnNameFound = true;
            				var cellsinCol = col.Cells;
            				foreach (var columncell in cellsinCol) 
            				{
            					if (columncell.RowIndex == rowIndexofFileName)
            					{
            						checkOut = columncell;
            						checkOutButtonFound = true;
            						Report.Screenshot(checkOut);
            						checkOut.Click();
            						sw.Stop();
            						Report.Info("Found Check Out button time elapsed " + sw.Elapsed);
            						break;
            					}
            				}
            			}
            		}
            		
            		
            	}
            	else
            	{
            		sw.Stop();
            		if (!checkOutButtonFound || !columnNameFound || !cellTextofFileNameFound)
            		{
            			Ranorex.Validate.Fail("Check out button, Column Name, or FileName Not Found. Check Out Button Found: " +checkOutButtonFound + " Column Name Found: " + columnNameFound + " File Name Found in Cell: " + cellTextofFileNameFound + " Time elapsed " + sw.Elapsed);
            		}
            		Ranorex.Validate.Fail("Table is not Visible. Time elapsed " + sw.Elapsed);
            	}
            } 
            catch (Exception ex) 
            {
            	
            	Report.Info("Cannot Find table.  Time elapsed " + sw.Elapsed + " Error: " + ex);
            }
            
        }

    }
}
