///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;
using System.IO;
using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;
using Microsoft.Office.Interop.Excel;

namespace ATS_BTP_TESTING
{
    public partial class ImportSheetNonReferencedFile
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        /// 
        public static string ProdStructNum {get; private set;}
        public static PopupWatcher missingRef {get; set;}
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void GetProdStructure()
        {
            Regex regex = new Regex(@"\d+");
        	Match match;
        	int indexofProdStructure = varTextofAC.LastIndexOf(":");
        	string ProdStructureNo = varTextofAC.Substring(indexofProdStructure + 2);
        	int len = ProdStructureNo.Length;
        	ProdStructureNo = ProdStructureNo.Substring(0,len - 2);
        	match = regex.Match(ProdStructureNo);
        	if (match.Success)
        	{
        		ProdStructNum = match.Value;
        		Ranorex.Report.Log(ReportLevel.Info, "Product Structure Number Created In PLM: " + ProdStructNum);
        	}
        	
        	else
        	{
        		Ranorex.Validate.Fail("Did not find an item number");
        	}
        }

        public void copyTestFiles()
        {
        	try
        	{
        		string SourcePath = @"\\ca01a9001\pgmis\Deployment_DEV\Ranorex\BTP_Test\test_files_BTP\BTP5.3\AddPart\AP25183698.SLDPRT";
            	string testFilesPath = @"C:\home\TC53";
            	string DestinationPath = testFilesPath + @"\AP25183698.SLDPRT";
           
            	varSheetPath = @"\\ca01a9001\pgmis\Deployment_DEV\Ranorex\BTP_Test\test_files_BTP - Copy\BTP5.3\ATSBTPTC5-3.xlsm";
            	
           	 	if (Directory.Exists(testFilesPath))
            	{
           	 		foreach (var file in new DirectoryInfo(testFilesPath).GetFiles()) 
           	 		{
           	 			File.SetAttributes(file.FullName, FileAttributes.Normal);
            		}
           	 		var dirInfo = new DirectoryInfo(testFilesPath);
           	 		dirInfo.Attributes = FileAttributes.Normal;           	 		
           	 		Directory.Delete(testFilesPath, true);
           	 		Report.Info($"Folder: {testFilesPath} successfully deleted");
            	}
           	 	else
	        	{
	        		Report.Info($"Folder: {testFilesPath} doesn't exist, can't delete");
	        	}
            	
           	 	Delay.Seconds(3.0);
            
            	Directory.CreateDirectory(testFilesPath);
            	Delay.Seconds(5.0);
            	File.Copy(SourcePath, DestinationPath, true);
        	}
        	
        	catch (Exception ex)
        	{
        		Ranorex.Validate.Fail(ex.StackTrace.ToString());
        	}
        	
            
        }

        public void PrepFile()
        {
        	string adamsIndexFile = @"\\ca01a9001\pgmis\Deployment_DEV\ATSPLM PLM Test Files\Generate Test Assembly\Test Files\Phantom2018\Index.txt";
        	var index = File.ReadAllText(adamsIndexFile);
        	string AssemblyFile = @"C:\home\test_files\Phantom2018\29635994-" + index + ".SLDASM";
        	string testFilesPath = @"C:\home\TC53";
            string DestinationPath = testFilesPath + @"\AP25183698.SLDPRT";
            BTPUserCodeCollection.AddModelToAssembly(AssemblyFile, DestinationPath);
            BTPUserCodeCollection.KillSW();
            Delay.Seconds(5.0);
            if (Directory.Exists(testFilesPath))
            	{
           	 		foreach (var file in new DirectoryInfo(testFilesPath).GetFiles()) 
           	 		{
           	 			File.SetAttributes(file.FullName, FileAttributes.Normal);
            		}
           	 		var dirInfo = new DirectoryInfo(testFilesPath);
           	 		dirInfo.Attributes = FileAttributes.Normal;           	 		
           	 		Directory.Delete(testFilesPath, true);
           	 		Report.Info($"Folder: {testFilesPath} successfully deleted");
            	}
           	 	else
	        	{
	        		Report.Info($"Folder: {testFilesPath} doesn't exist, can't delete");
	        	}
           
        }
        

        public void PopupWatcherMissingReferences()
        {
        	missingRef = new PopupWatcher();
        	missingRef.Watch(repo.MessageDialog.SelfInfo, TakeScreenshot);
      //  	missingRef.WatchAndClick(repo.MessageDialog.SelfInfo, repo.MessageDialog.YesButtonInfo);
        	missingRef.Start();
        }
        
        public static void TakeScreenshot(Ranorex.Core.Repository.RepoItemInfo repoItemInfo, Ranorex.Core.Element repoElement)
        {
        	if(repoItemInfo == repo.MessageDialog.SelfInfo)
        	{
     			Report.Info("Found Popup for missing References, clicking Yes");
     			Report.Screenshot();
        		Delay.Seconds(2.0);
        		repo.MessageDialog.YesButton.Click();
        		
        	}
        }

        public void StopPW()
        {
        	missingRef.Stop();
        }

    }
}
