///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace PLM_Mechanical_Deploy_Test
{
    public partial class SetVariables
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void SetGlobalVariables()
        {
        	string temp = "";
        	
        	try
        	{   
				// set username        		
	        	temp = TestSuite.Current.Parameters["RunEnvironment"] + " Username";
	        	Username = GlobalProjectData.GetConfigurationValue(temp);
	        	// set password
	            temp = TestSuite.Current.Parameters["RunEnvironment"] + " Password";
	        	Password = GlobalProjectData.GetConfigurationValue(temp);
	        	// set pm username
	        	temp = TestSuite.Current.Parameters["RunEnvironment"] + " PMUsername";
	        	PMUsername = GlobalProjectData.GetConfigurationValue(temp);
	        	Report.Log(ReportLevel.Info, "PMUsername set to: " + PMUsername);
	        	// set pm password
	            temp = TestSuite.Current.Parameters["RunEnvironment"] + " PMPassword";
	        	PMPassword = GlobalProjectData.GetConfigurationValue(temp);
	        	Report.Log(ReportLevel.Info, "PMPassword set to: " + PMPassword);
	        	// set gps username
	        	temp = TestSuite.Current.Parameters["RunEnvironment"] + " GPSUsername";
	        	GPSUsername = GlobalProjectData.GetConfigurationValue(temp);
	        	// set gps password
	            temp = TestSuite.Current.Parameters["RunEnvironment"] + " GPSPassword";
	        	GPSPassword = GlobalProjectData.GetConfigurationValue(temp);
	        	// set pzm user
	        	temp = TestSuite.Current.Parameters["RunEnvironment"] + " PZMUsername";
	        	PZMUsername = GlobalProjectData.GetConfigurationValue(temp);
	        	// set pzm password
	            temp = TestSuite.Current.Parameters["RunEnvironment"] + " PZMPassword";
	        	PZMPassword = GlobalProjectData.GetConfigurationValue(temp);
	        	// set gps url
	            temp = TestSuite.Current.Parameters["RunEnvironment"] + " GPSURL";
	        	GPSURL = GlobalProjectData.GetConfigurationValue(temp);
	        	// set pzm url
	            temp = TestSuite.Current.Parameters["RunEnvironment"] + " PZMURL";
	        	PZMURL = GlobalProjectData.GetConfigurationValue(temp);
        	}
        	
        	catch (ArgumentException ex)
        	{
        	    Report.Log(ReportLevel.Failure, "Error setting property: " + temp + "\n" + ex.ToString());	
        	}
        	
        	catch (Exception x)
        	{
        		Report.Log(ReportLevel.Failure, "Error setting property: " + temp + "\n" + x.ToString());
        	}

        }

    }
}
