///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;
using System.Linq;
using System.IO;
using System.IO.Compression;
using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace PLM_Mechanical_Deploy_Test
{
    public partial class PWO_Validation
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void Validate_PWO_Files()
        {
        	try
        	{
        		System.Diagnostics.Process.Start(varPWODirectory);
        		string PWOname = varPWOName.Substring(varPWOName.IndexOf("PWO"));
        		varRepoPWOName = PWOname;
        		string pwoZipFilePath = varPWODirectory + "\\" + PWOname + ".zip";
        		string pwoUnzipPath = varPWODirectory + "\\" + PWOname;
        		Directory.CreateDirectory(pwoUnzipPath);
        		ZipFile.ExtractToDirectory(pwoZipFilePath, pwoUnzipPath);
        		
        		var FilteredFilelist = new DirectoryInfo(@"\\ca01a9001\pgmis\Deployment_DEV\Ranorex\PWO Test\PWO13563").GetFiles("*.*", SearchOption.AllDirectories).Where(f => !f.Attributes.HasFlag(FileAttributes.Hidden)).ToArray();
        		var FilteredFilelist2 = new DirectoryInfo(pwoUnzipPath).GetFiles("*.*", SearchOption.AllDirectories).Where(f => !f.Attributes.HasFlag(FileAttributes.Hidden)).ToArray();
        		int length = FilteredFilelist.Length;
        		int length2 = FilteredFilelist2.Length;
        		if (length != length2)
        		{
        			Ranorex.Validate.Fail("The Files generated by the PWO do not match!");
        		}
        		for (int i=0; i<length; i++)
        		{
        			if (FilteredFilelist[i].Name == FilteredFilelist2[i].Name)
        			{
        				Ranorex.Validate.IsTrue(true,"Filename: " + FilteredFilelist2[i].Name + " Matches");
        			}
        			else
        			{
        				Ranorex.Validate.Fail("The Files generated by the PWO do not match!");
        				break;
        			}
        		}
        		System.Diagnostics.Process.Start(pwoUnzipPath);
        		Delay.Milliseconds(1000);
        		Report.Screenshot(ReportLevel.Info, "User", "", repo.PWO13563.Self, false, new RecordItemIndex(41));
            
        	}
        	
        	catch (Exception ex)
        	{
        		Ranorex.Validate.Fail("Error: " + ex);
        	}
            	
        }

    }
}
