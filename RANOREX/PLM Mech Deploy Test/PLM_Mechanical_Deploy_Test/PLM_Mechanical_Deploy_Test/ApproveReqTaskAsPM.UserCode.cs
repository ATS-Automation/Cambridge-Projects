///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
// 
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Drawing;
using System.Text;
using System.Text.RegularExpressions;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace PLM_Mechanical_Deploy_Test
{
    public partial class ApproveReqTaskAsPM
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void RefreshandCheck()
        {
        	if(!repo.PLMMainForm.MainSplitPane.DashboardPane.MyAssignments.ReqTaskInfo.Exists(10000))
        	{
        		int i=0;
        		do
        		{
        			Report.Log(ReportLevel.Info, "Did not find Req: " + ReqNumber + " Pressing Refresh...");
        			repo.Java2.AbstractThreadedPanelDollarRefreshButton.Click();
        			Delay.Seconds(30.0);
        			i++;
        		
        		}while(!repo.PLMMainForm.MainSplitPane.DashboardPane.MyAssignments.ReqTaskInfo.Exists(10000)&&i<8);
        	}
        }

        public void CheckMRPFailed()
        {
            try 
            {
            	 repo.PLMMainForm.MainSplitPane.WorkOrdersPane.StatusEqApprovedInfo.WaitForExists(300000); 
            	 Report.Log(ReportLevel.Info, "Found Status Approved");
            	 Report.Log(ReportLevel.Info,"Text value of ReqStatus item: " + repo.PLMMainForm.MainSplitPane.WorkOrdersPane.ReqStatus.TextValue.ToString());
            }
                     		 
            catch (Exception) 
            {
            	do
            	{
            		
            	
            	if(repo.PLMMainForm.MainSplitPane.WorkOrdersPane.ReqStatus.TextValue.ToString().Contains("Approval Pending"))
            	 {
            		Report.Log(ReportLevel.Info, "Found Status Approval Pending, clicking Dashboard and reloading Req...");
            		repo.PLMMainForm.btnDashboard.Click();
            		repo.PLMMainForm.MainSplitPane.DashboardPane.MyAssignments.ReqTaskInfo.WaitForExists(60000);
            		repo.PLMMainForm.MainSplitPane.DashboardPane.MyAssignments.ReqTask.DoubleClick();
            		repo.PLMMainForm.MainSplitPane.WorkOrdersPane.ReqStatusInfo.WaitForExists(30000);
            	 }
            	}while(repo.PLMMainForm.MainSplitPane.WorkOrdersPane.ReqStatus.TextValue.ToString().Contains("Approval Pending"));
            	
            	
            	if (repo.PLMMainForm.MainSplitPane.WorkOrdersPane.StatusMRPFailedInfo.Exists())
            	{
            		int i = 0;
            		do
            		{
            			Report.Log(ReportLevel.Info, "Received Failed To Validate MRP Project, retrying...");
            			repo.PLMMainForm.MainSplitPane.Approve.Click();
            			try
            			{
            				repo.PLMMainForm.MainSplitPane.WorkOrdersPane.StatusEqApprovedInfo.WaitForExists(300000);
            			}
            			catch (Exception ex)
            			{
            				Report.Log(ReportLevel.Warn, "Error: " + ex.Message.ToString() + " Retrying...");
            			}
            			i++;
            			Report.Log(ReportLevel.Info,"Text value of ReqStatus item: " + repo.PLMMainForm.MainSplitPane.WorkOrdersPane.ReqStatus.TextValue.ToString());
            			
            		}while(!repo.PLMMainForm.MainSplitPane.WorkOrdersPane.ReqStatus.TextValue.ToString().Contains("Approved")&&i<3);
            	}
            	
            
            }
        }

    }
}